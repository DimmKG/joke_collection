# Joke collection

Node.JS application, that gets a joke from [Joke API](https://v2.jokeapi.dev/joke/Any) and prints it to the console

## Build application

```bash
$ npm run build
```

## Usage

```bash
$ npm start -- --help
Usage: joke_collection [options]

Get a random joke from Joke API "https://v2.jokeapi.dev/"

Options:
  -V, --version                   output the version number
  -c, --category [categories...]  specify category
  -h, --help                      display help for command
```

## Run benchmark

```bash
$ npm run benchmark
```
